 
 
 
 
 
 

import UIKit
import AVFoundation

class chngRewriteGiga: lrnRespondCall {
    var nmersRemainWeek = false
    var thcknessCancelFont = false
    var rchveHoldSpace:TimeInterval!
    var chrAmountCard:drivWord!
    var sterskGigaExist:NSIndexPath!
    
     
    var bserverMagnifierNext:UIView!
    var pctreButtonOmit:UIView!
    var verwrteFlushDecoder:AVPlayer!
    var strgeCareWorld:AVPlayerLayer!
    var reserveCenterRegard:AVPlayerItem!
    var pltfrmLittleRight:UIActivityIndicatorView!
    var chnnelsHyphenLike:Timer!
    var ntteExpandBack:Timer!
     
    var trndFineRegard:UIView!
    
    override func keywrdNameSince() {
        self.backgroundColor = .lbrryRemainPath(trnsfrmUnitUser: 0x000000, ntteBelowNest: 0.5)
        self.isHidden = true
    }
    
    func tensnDictSwap() {
        let trncteOnceSublayer:[String:Any] = self.chrAmountCard.pperLowestTerm[(self.sterskGigaExist == nil ? 0 : self.sterskGigaExist.row)]

        bserverMagnifierNext = UIView()
        bserverMagnifierNext.backgroundColor = .CnfrmRoomRange
        self.addSubview(bserverMagnifierNext)
        bserverMagnifierNext.snp.makeConstraints { make in
            make.bottom.left.right.equalToSuperview()
        }
                
        let regexAcceptLess = UIButton(type: .custom)
        regexAcceptLess.backgroundColor = .clear
        regexAcceptLess.addTarget(self, action: #selector(restreBorderCare), for: .touchUpInside)
        bserverMagnifierNext.addSubview(regexAcceptLess)
        regexAcceptLess.snp.makeConstraints { make in
            make.top.left.right.equalToSuperview()
            make.height.equalTo(self.frame.size.width * 4 / 7)
        }

        let pnnCenterWatch = trncteOnceSublayer[tensnBottomSpeech([434, 431, 436, 433])]
        if (self.chrAmountCard.exmpleLongDamage == "1" && pnnCenterWatch != nil && !(pnnCenterWatch is NSNull) && pnnCenterWatch as! String != "") {
            let eventToneWrong : URL = URL(string: (pnnCenterWatch as! String))!
            reserveCenterRegard = AVPlayerItem(url: eventToneWrong)
            verwrteFlushDecoder = AVPlayer(playerItem: reserveCenterRegard)
            verwrteFlushDecoder.isMuted = true
            strgeCareWorld = AVPlayerLayer(player: verwrteFlushDecoder)
            nywhereComeEntry(dsmssSystemMoment: true)
            strgeCareWorld.frame = CGRect(x: 0, y: 0, width: self.frame.size.width, height: self.frame.size.width * 4 / 7)
            regexAcceptLess.layer.addSublayer(strgeCareWorld)
            try? AVAudioSession.sharedInstance().setCategory(.playback)

            let cnfrmSettleDialog = UIButton(type: .custom).dscssPriceDeselected(certnAgainProxy("359"), for: .normal).dscssPriceDeselected(certnAgainProxy("360"), for: .selected)
            cnfrmSettleDialog.backgroundColor = .clear
            cnfrmSettleDialog.addTarget(self, action: #selector(lnggeTupleMiddle), for: .touchUpInside)
            regexAcceptLess.addSubview(cnfrmSettleDialog)
            cnfrmSettleDialog.snp.makeConstraints { make in
                make.top.equalTo(regexAcceptLess.snp.top).offset(10)
                make.right.equalTo(regexAcceptLess.snp.right).offset(-10)
                make.height.width.equalTo(35)
            }
            
            let mnyCodeDepend = UIView()
            mnyCodeDepend.backgroundColor = .lightGray
            regexAcceptLess.addSubview(mnyCodeDepend)
            mnyCodeDepend.snp.makeConstraints { make in
                make.bottom.equalTo(regexAcceptLess.snp.bottom).offset(-3)
                make.left.right.equalToSuperview()
                make.height.equalTo(3)
            }
            
            let prcedreBlueBlock:String = trncteOnceSublayer[tensnBottomSpeech([438, 429])] as! String
            let msmtchWidthRange:Float = Float(prcedreBlueBlock) ?? 0.0
            let sggestRightRange:CGFloat = CGFloat(msmtchWidthRange)/100.0
            let lthghStreamZero = UIView()
            lthghStreamZero.backgroundColor = UIColor.lbrryRemainPath(trnsfrmUnitUser: 0x39CC91)
            regexAcceptLess.addSubview(lthghStreamZero)
            lthghStreamZero.snp.makeConstraints { make in
                make.bottom.equalTo(regexAcceptLess.snp.bottom).offset(-3)
                make.height.equalTo(3)
                make.left.equalToSuperview()
                make.width.equalTo(self.frame.size.width * sggestRightRange)
            }
        }
        
        pctreButtonOmit = UIView()
        pctreButtonOmit.backgroundColor = .clear
        regexAcceptLess.addSubview(pctreButtonOmit)
        pctreButtonOmit.snp.makeConstraints { make in
            make.edges.equalToSuperview()
        }
        
        let ncldeMoveField = UIImageView()
        ncldeMoveField.contentMode = .scaleAspectFill
        ncldeMoveField.backgroundColor = .clear
        ncldeMoveField.clipsToBounds = true
        pctreButtonOmit.addSubview(ncldeMoveField)
        ncldeMoveField.snp.makeConstraints { make in
            make.edges.equalToSuperview()
        }
        
        let nshftMindInvoke = UIVisualEffectView(effect: UIBlurEffect(style: .dark))
        nshftMindInvoke.alpha = 0.7
        nshftMindInvoke.isHidden = true
        pctreButtonOmit.addSubview(nshftMindInvoke)
        nshftMindInvoke.snp.makeConstraints { make in
            make.edges.equalToSuperview()
        }
        
        let ddressCreateInvoke = UIImageView()
        ddressCreateInvoke.layer.cornerRadius = 8
        ddressCreateInvoke.clipsToBounds = true
        ddressCreateInvoke.contentMode = .scaleAspectFit
        ddressCreateInvoke.backgroundColor = .clear
        ddressCreateInvoke.isHidden = true
        pctreButtonOmit.addSubview(ddressCreateInvoke)
        ddressCreateInvoke.snp.makeConstraints { make in
            make.edges.equalToSuperview()
        }
        
        if (self.chrAmountCard.exmpleLongDamage == "1" && pnnCenterWatch != nil && !(pnnCenterWatch is NSNull) && pnnCenterWatch as! String != "") {
            pltfrmLittleRight = UIActivityIndicatorView(style: .whiteLarge)
            pltfrmLittleRight.startAnimating()
            pctreButtonOmit.addSubview(pltfrmLittleRight)
            pltfrmLittleRight.snp.makeConstraints { make in
                make.center.equalToSuperview();
                make.width.height.equalTo(100);
            }
        }
        
        let cntnFlagInput = UIButton(type: .custom)
        cntnFlagInput.frame = CGRectMake(10, 10, 60, 24)
        cntnFlagInput.backgroundColor = .lbrryRemainPath(trnsfrmUnitUser: 0x000000, ntteBelowNest: 0.5)
        cntnFlagInput.titleLabel?.font = UIFont.systemFont(ofSize: 10)
        cntnFlagInput.setImage(UIImage(named: "scndRecognizeSide"), for: .normal)
        cntnFlagInput.setTitle(tensnBottomSpeech([402, 399, 412, 395]), for: .normal)
        cntnFlagInput.setTitleColor(.lbrryRemainPath(trnsfrmUnitUser: 0x3DDAF3), for: .normal)
        cntnFlagInput.layer.cornerRadius = 12
        cntnFlagInput.clipsToBounds = true
        var reserveStuffFact:[UIImage] = []
        reserveStuffFact.append(UIImage(named: "scndRecognizeSide")!)
        reserveStuffFact.append(UIImage(named: "singlMaximumEach")!)
        reserveStuffFact.append(UIImage(named: "fncyParallelLine")!)
        cntnFlagInput.imageView?.animationImages = reserveStuffFact
        cntnFlagInput.imageView?.animationDuration = Double(reserveStuffFact.count) * 0.3
        cntnFlagInput.imageView?.animationRepeatCount = 0
        cntnFlagInput.imageView?.startAnimating()
        regexAcceptLess.addSubview(cntnFlagInput)
        
        let crctFieldAway = UIButton(type: .custom)
        crctFieldAway.backgroundColor = .clear
        crctFieldAway.addTarget(self, action: #selector(mnyAtomicallyDepth), for: .touchUpInside)
        bserverMagnifierNext.addSubview(crctFieldAway)
        crctFieldAway.snp.makeConstraints { make in
            make.top.equalTo(regexAcceptLess.snp.bottom).offset(0)
            make.left.right.equalToSuperview()
            make.height.equalTo(80)
        }
        
        let nmersDepthUpon = UIImageView()
        nmersDepthUpon.backgroundColor = .clear
        let eventToneWrong : URL = URL(string: self.chrAmountCard.cmpseWiseBaseline)!
        nmersDepthUpon.kf.setImage(with: eventToneWrong, placeholder: UIImage(named: "chnkAdvanceTake"))
        crctFieldAway.addSubview(nmersDepthUpon)
        nmersDepthUpon.snp.makeConstraints { make in
            make.top.equalTo(ncldeMoveField.snp.bottom).offset(15)
            make.left.equalToSuperview().offset(15)
            make.width.height.equalTo(50)
        }
        
        let nterpretSchemaOnce = UIImageView().dscssPriceDeselected(certnAgainProxy("358"))
        nterpretSchemaOnce.backgroundColor = .clear
        crctFieldAway.addSubview(nterpretSchemaOnce)
        nterpretSchemaOnce.snp.makeConstraints { make in
            make.top.equalTo(ncldeMoveField.snp.bottom).offset(20)
            make.right.equalToSuperview().offset(-15)
            make.width.height.equalTo(40)
        }
        
        let cntrlWordShell = UILabel()
        cntrlWordShell.textColor = .SrceShiftDecoder
        cntrlWordShell.numberOfLines = 2;
        cntrlWordShell.adjustsFontSizeToFitWidth = true
        cntrlWordShell.font = .systemFont(ofSize: 18, weight: .bold)
        cntrlWordShell.text = self.chrAmountCard.gryBorderSize
        crctFieldAway.addSubview(cntrlWordShell)
        cntrlWordShell.snp.makeConstraints { make in
            make.left.equalTo(nmersDepthUpon.snp.right).offset(12)
            make.right.equalTo(nterpretSchemaOnce.snp.left).offset(-12)
            make.top.equalTo(ncldeMoveField.snp.bottom).offset(5)
            make.height.equalTo(40)
        }
        
        let perteTimesWell = UILabel()
        perteTimesWell.textColor = .RgnUniqueData
        perteTimesWell.adjustsFontSizeToFitWidth = true
        perteTimesWell.font = .systemFont(ofSize: 15, weight: .bold)
        perteTimesWell.text = self.chrAmountCard.cmbneAfterTouch
        crctFieldAway.addSubview(perteTimesWell)
        perteTimesWell.snp.makeConstraints { make in
            make.left.equalTo(nmersDepthUpon.snp.right).offset(12)
            make.right.equalTo(nterpretSchemaOnce.snp.left).offset(-12)
            make.top.equalTo(cntrlWordShell.snp.bottom).offset(0)
            make.height.equalTo(25)
        }
        
        let prevsFlattenBound = UIView()
        prevsFlattenBound.backgroundColor = .RgnUniqueData
        crctFieldAway.addSubview(prevsFlattenBound)
        prevsFlattenBound.snp.makeConstraints { make in
            make.width.equalTo(self.frame.size.width)
            make.bottom.equalToSuperview().offset(-0.5)
            make.height.equalTo(0.5)
        }
        
        var specfcStaySmooth = trncteOnceSublayer[tensnBottomSpeech([425, 437, 444, 427, 440])]
        if (specfcStaySmooth != nil && !(specfcStaySmooth is NSNull) && specfcStaySmooth as! String != "") {
            let eventToneWrong : URL = URL(string: (specfcStaySmooth as! String))!
            ncldeMoveField.kf.setImage(with: eventToneWrong, placeholder: UIImage(named: "chnkAdvanceTake"))
            ncldeMoveField.snp.remakeConstraints { make in
                make.edges.equalToSuperview()
            }
            let cmmentMuchClick : String = trncteOnceSublayer[tensnBottomSpeech([435, 444])] as! String
            if (cmmentMuchClick == "1") {
                ddressCreateInvoke.isHidden = false
                nshftMindInvoke.isHidden = false
                ddressCreateInvoke.kf.setImage(with: eventToneWrong)
            }
        } else {
            specfcStaySmooth = trncteOnceSublayer[tensnBottomSpeech([434, 437, 429, 437])] ?? ""
            let eventToneWrong : URL = URL(string: (specfcStaySmooth as! String))!
            ncldeMoveField.contentMode = .scaleAspectFit
            ncldeMoveField.kf.setImage(with: eventToneWrong)
            ncldeMoveField.snp.remakeConstraints { make in
                make.top.bottom.equalToSuperview()
                make.left.equalToSuperview().offset(20)
                make.right.equalToSuperview().offset(-20)
            }
        }
        
        let cmpreBrushHeap = UILabel()
        cmpreBrushHeap.backgroundColor = .clear
        cmpreBrushHeap.numberOfLines = 2;
        cmpreBrushHeap.textAlignment = .center
        cmpreBrushHeap.adjustsFontSizeToFitWidth = true
        let replceLogicWait = "\(trncteOnceSublayer[tensnBottomSpeech([442, 431, 442, 434, 427])] ?? "")"
        let excldeBoardBook = replceLogicWait + " \(self.chrAmountCard.ntteEdgeUndone)"
        let plcementPauseSoft = NSMutableAttributedString.init(string: excldeBoardBook)
        plcementPauseSoft.addAttribute(NSAttributedString.Key.font, value: UIFont.systemFont(ofSize: 19, weight: .bold) ,range: NSRange.init(location: 0, length: excldeBoardBook.count))
        plcementPauseSoft.addAttribute(NSAttributedString.Key.foregroundColor, value: UIColor.white, range: NSRange.init(location: 0, length: excldeBoardBook.count))
        plcementPauseSoft.addAttribute(NSAttributedString.Key.foregroundColor, value: UIColor.lbrryRemainPath(trnsfrmUnitUser: 0x00D26C), range: NSRange.init(location: 0, length: replceLogicWait.count))
        cmpreBrushHeap.attributedText = plcementPauseSoft
        bserverMagnifierNext.addSubview(cmpreBrushHeap)
        cmpreBrushHeap.snp.makeConstraints { make in
            make.top.equalTo(crctFieldAway.snp.bottom).offset(20)
            make.left.equalToSuperview().offset(20)
            make.right.equalToSuperview().offset(-20)
            make.height.equalTo(80)
        }

        let memryCenterOccur = tchConfusePlus(type: .custom)
        if (self.chrAmountCard.ndexSequenceCaret != "1") {
            memryCenterOccur.backgroundColor = UIColor.lbrryRemainPath(trnsfrmUnitUser: 0x39CC91)
            memryCenterOccur.titleLabel?.font = .systemFont(ofSize: 18, weight: .bold)
            memryCenterOccur.layer.cornerRadius = 25
            memryCenterOccur.clipsToBounds = true
            memryCenterOccur.setTitle((lvlProduceRoot.default.selessLargeIncrement() ? self.chrAmountCard.refrmtViewRadius : self.chrAmountCard.nvldRightError), for: .normal)
            memryCenterOccur.addTarget(self, action: #selector(elementWeekAppear), for: .touchUpInside)
            bserverMagnifierNext.addSubview(memryCenterOccur)
            memryCenterOccur.snp.makeConstraints { make in
                make.top.equalTo(cmpreBrushHeap.snp.bottom).offset(25)
                make.left.equalTo(bserverMagnifierNext.snp.left).offset(40)
                make.right.equalTo(bserverMagnifierNext.snp.right).offset(-40)
                make.height.equalTo(50)
            }
        }
        
        if (self.chrAmountCard.dsmssShieldTrue == "1") {
            let nfnteEvenWatch = UILabel()
            nfnteEvenWatch.textColor = .RgnUniqueData
            nfnteEvenWatch.font = .systemFont(ofSize: 16, weight: .bold)
            nfnteEvenWatch.text = self.chrAmountCard.dscssBlankWant
            nfnteEvenWatch.textAlignment = .center
            regexAcceptLess.addSubview(nfnteEvenWatch)
            nfnteEvenWatch.snp.makeConstraints { make in
                make.left.equalTo(bserverMagnifierNext.snp.left).offset(20)
                make.right.equalTo(bserverMagnifierNext.snp.right).offset(-20)
                make.bottom.equalTo(bserverMagnifierNext.snp.bottom).offset(-30)
                if (self.chrAmountCard.ndexSequenceCaret != "1") {
                    make.top.equalTo(memryCenterOccur.snp.bottom).offset(20)
                } else {
                    make.top.equalTo(cmpreBrushHeap.snp.bottom).offset(20)
                }
                make.height.equalTo(30)
            }
        } else {
            let estercSureAngle = UIButton(type: .custom)
            estercSureAngle.backgroundColor = .clear
            estercSureAngle.addTarget(self, action: #selector(resltNamedPlus), for: .touchUpInside)
            let cptreExistAttributes = NSNumber(integerLiteral: NSUnderlineStyle.single.rawValue)
            let plcementPauseSoft = NSMutableAttributedString.init(string: self.chrAmountCard.sbclssMatrixDouble)
            plcementPauseSoft.addAttribute(NSAttributedString.Key.underlineStyle, value:cptreExistAttributes ,range: NSRange.init(location: 0, length: self.chrAmountCard.sbclssMatrixDouble.count))
            estercSureAngle.setAttributedTitle(plcementPauseSoft, for: .normal)
            estercSureAngle.setTitleColor(.RgnUniqueData, for: .normal)
            bserverMagnifierNext.addSubview(estercSureAngle)
            estercSureAngle.snp.makeConstraints { make in
                make.left.equalTo(bserverMagnifierNext.snp.left).offset(20)
                make.right.equalTo(bserverMagnifierNext.snp.right).offset(-20)
                make.bottom.equalTo(bserverMagnifierNext.snp.bottom).offset(-30)
                if (self.chrAmountCard.ndexSequenceCaret != "1") {
                    make.top.equalTo(memryCenterOccur.snp.bottom).offset(20)
                } else {
                    make.top.equalTo(cmpreBrushHeap.snp.bottom).offset(20)
                }
                make.height.equalTo(30)
            }
        }
    }
    
    func repntClickCase() {
        let trncteOnceSublayer:[String:Any] = self.chrAmountCard.pperLowestTerm[(self.sterskGigaExist == nil ? 0 : self.sterskGigaExist.row)]

        trndFineRegard = UIView()
        trndFineRegard.backgroundColor = .clear
        self.addSubview(trndFineRegard)
        trndFineRegard.snp.makeConstraints { make in
            make.bottom.left.right.equalToSuperview()
        }
        
        let msmtchWidthRange = self.frame.size.width - 140
        let lredyCodeRecord = UIView()
        lredyCodeRecord.backgroundColor = .CnfrmRoomRange
        trndFineRegard.addSubview(lredyCodeRecord)
        lredyCodeRecord.snp.makeConstraints { make in
            make.top.equalToSuperview().offset(msmtchWidthRange * 4 / 7 / 3 * 2)
            make.bottom.left.right.equalToSuperview()
        }
        
        let regexAcceptLess = UIButton(type: .custom)
        regexAcceptLess.backgroundColor = .clear
        regexAcceptLess.layer.cornerRadius = 8
        regexAcceptLess.clipsToBounds = true
        regexAcceptLess.addTarget(self, action: #selector(redndntSwapTimer), for: .touchUpInside)
        trndFineRegard.addSubview(regexAcceptLess)
        regexAcceptLess.snp.makeConstraints { make in
            make.top.equalToSuperview()
            make.centerX.equalToSuperview()
            make.width.equalTo(msmtchWidthRange)
            make.height.equalTo(msmtchWidthRange * 4 / 7)
        }
        
        let ncldeMoveField = UIImageView()
        ncldeMoveField.contentMode = .scaleAspectFill
        ncldeMoveField.backgroundColor = .clear
        ncldeMoveField.clipsToBounds = true
        regexAcceptLess.addSubview(ncldeMoveField)
        ncldeMoveField.snp.makeConstraints { make in
            make.edges.equalToSuperview()
        }
        
        let nshftMindInvoke = UIVisualEffectView(effect: UIBlurEffect(style: .dark))
        nshftMindInvoke.alpha = 0.7
        nshftMindInvoke.isHidden = true
        regexAcceptLess.addSubview(nshftMindInvoke)
        nshftMindInvoke.snp.makeConstraints { make in
            make.edges.equalToSuperview()
        }
        
        let ddressCreateInvoke = UIImageView()
        ddressCreateInvoke.layer.cornerRadius = 8
        ddressCreateInvoke.clipsToBounds = true
        ddressCreateInvoke.contentMode = .scaleAspectFit
        ddressCreateInvoke.backgroundColor = .clear
        ddressCreateInvoke.isHidden = true
        regexAcceptLess.addSubview(ddressCreateInvoke)
        ddressCreateInvoke.snp.makeConstraints { make in
            make.edges.equalToSuperview()
        }
        
        var specfcStaySmooth = trncteOnceSublayer[tensnBottomSpeech([425, 437, 444, 427, 440])]
        if (specfcStaySmooth != nil && !(specfcStaySmooth is NSNull) && specfcStaySmooth as! String != "") {
            let eventToneWrong : URL = URL(string: (specfcStaySmooth as! String))!
            ncldeMoveField.kf.setImage(with: eventToneWrong, placeholder: UIImage(named: "chnkAdvanceTake"))
            ncldeMoveField.snp.remakeConstraints { make in
                make.edges.equalToSuperview()
            }
            let cmmentMuchClick : String = trncteOnceSublayer[tensnBottomSpeech([435, 444])] as! String
            if (cmmentMuchClick == "1") {
                ddressCreateInvoke.isHidden = false
                nshftMindInvoke.isHidden = false
                ddressCreateInvoke.kf.setImage(with: eventToneWrong)
            }
        } else {
            specfcStaySmooth = trncteOnceSublayer[tensnBottomSpeech([434, 437, 429, 437])] ?? ""
            let eventToneWrong : URL = URL(string: (specfcStaySmooth as! String))!
            ncldeMoveField.contentMode = .scaleAspectFit
            ncldeMoveField.kf.setImage(with: eventToneWrong)
            ncldeMoveField.snp.remakeConstraints { make in
                make.top.bottom.equalToSuperview()
                make.left.equalToSuperview().offset(20)
                make.right.equalToSuperview().offset(-20)
            }
        }
        
        let estercSureAngle = UIButton(type: .custom)
        estercSureAngle.backgroundColor = .clear
        estercSureAngle.setImage(UIImage(named: "psPertainRoot"), for: .normal)
        estercSureAngle.addTarget(self, action: #selector(pckgeAboutConfig), for: .touchUpInside)
        lredyCodeRecord.addSubview(estercSureAngle)
        estercSureAngle.snp.makeConstraints { make in
            make.right.equalTo(lredyCodeRecord.snp.right).offset(-10)
            make.top.equalTo(lredyCodeRecord.snp.top).offset(10)
            make.height.width.equalTo(30)
        }
        
        let cmpreBrushHeap = UILabel()
        cmpreBrushHeap.textColor = .white
        cmpreBrushHeap.textAlignment = .center
        cmpreBrushHeap.adjustsFontSizeToFitWidth = true
        cmpreBrushHeap.font = .systemFont(ofSize: 19, weight: .bold)
        cmpreBrushHeap.text = self.chrAmountCard.selectReadyWill
        trndFineRegard.addSubview(cmpreBrushHeap)
        cmpreBrushHeap.snp.makeConstraints { make in
            make.left.equalTo(trndFineRegard.snp.left).offset(12)
            make.right.equalTo(trndFineRegard.snp.right).offset(-12)
            make.top.equalTo(regexAcceptLess.snp.bottom).offset(25)
            make.height.equalTo(40)
        }
        
        let cntrlWordShell = UILabel()
        cntrlWordShell.textColor = UIColor.lbrryRemainPath(trnsfrmUnitUser: 0x00D26C)
        cntrlWordShell.numberOfLines = 2;
        cntrlWordShell.textAlignment = .center
        cntrlWordShell.adjustsFontSizeToFitWidth = true
        cntrlWordShell.font = .systemFont(ofSize: 21, weight: .bold)
        let replceLogicWait = "\(trncteOnceSublayer[tensnBottomSpeech([442, 431, 442, 434, 427])] ?? "")"
        cntrlWordShell.text = replceLogicWait
        trndFineRegard.addSubview(cntrlWordShell)
        cntrlWordShell.snp.makeConstraints { make in
            make.left.equalTo(trndFineRegard.snp.left).offset(12)
            make.right.equalTo(trndFineRegard.snp.right).offset(-12)
            make.top.equalTo(cmpreBrushHeap.snp.bottom).offset(5)
            make.height.equalTo(40)
        }
        
        let perteTimesWell = UILabel()
        perteTimesWell.textColor = .RgnUniqueData
        perteTimesWell.numberOfLines = 2;
        perteTimesWell.textAlignment = .center
        perteTimesWell.adjustsFontSizeToFitWidth = true
        perteTimesWell.font = .systemFont(ofSize: 15, weight: .bold)
        perteTimesWell.text = self.chrAmountCard.semclnBrowseMany
        trndFineRegard.addSubview(perteTimesWell)
        perteTimesWell.snp.makeConstraints { make in
            make.left.equalTo(trndFineRegard.snp.left).offset(12)
            make.right.equalTo(trndFineRegard.snp.right).offset(-12)
            make.top.equalTo(cntrlWordShell.snp.bottom).offset(15)
            make.height.equalTo(50)
            if (self.chrAmountCard.specfcNatureEcho == "1") {
                make.bottom.equalTo(trndFineRegard.snp.bottom).offset(-40)
            }
        }
        
        if (self.chrAmountCard.specfcNatureEcho != "1") {
            let memryCenterOccur = tchConfusePlus(type: .custom)
            memryCenterOccur.backgroundColor = UIColor.lbrryRemainPath(trnsfrmUnitUser: 0x39CC91)
            memryCenterOccur.layer.cornerRadius = 25
            memryCenterOccur.clipsToBounds = true
            memryCenterOccur.addTarget(self, action: #selector(nhbtExtendDisc), for: .touchUpInside)
            trndFineRegard.addSubview(memryCenterOccur)
            memryCenterOccur.snp.makeConstraints { make in
                make.top.equalTo(perteTimesWell.snp.bottom).offset(25)
                make.left.equalTo(trndFineRegard.snp.left).offset(40)
                make.right.equalTo(trndFineRegard.snp.right).offset(-40)
                make.bottom.equalTo(trndFineRegard.snp.bottom).offset(-40)
                make.height.equalTo(50)
            }
            
            let ppsteShipCircle = UILabel()
            ppsteShipCircle.textColor = .white
            ppsteShipCircle.adjustsFontSizeToFitWidth = true
            ppsteShipCircle.textAlignment = .center
            ppsteShipCircle.font = .systemFont(ofSize: 18, weight: .bold)
            ppsteShipCircle.text = self.chrAmountCard.ttlCellsScreen
            memryCenterOccur.addSubview(ppsteShipCircle)
            ppsteShipCircle.snp.makeConstraints { make in
                make.centerX.top.bottom.equalToSuperview()
            }
            
            let nmersDepthUpon = UIImageView()
            nmersDepthUpon.backgroundColor = .clear
            nmersDepthUpon.layer.borderColor = UIColor.white.cgColor
            nmersDepthUpon.layer.borderWidth = 1
            nmersDepthUpon.layer.cornerRadius = 6
            nmersDepthUpon.clipsToBounds = true
            let eventToneWrong : URL = URL(string: self.chrAmountCard.cmpseWiseBaseline)!
            nmersDepthUpon.kf.setImage(with: eventToneWrong, placeholder: UIImage(named: "chnkAdvanceTake"))
            memryCenterOccur.addSubview(nmersDepthUpon)
            nmersDepthUpon.snp.makeConstraints { make in
                make.right.equalTo(ppsteShipCircle.snp.left).offset(-15)
                make.centerY.equalToSuperview()
                make.width.height.equalTo(32)
            }
        }
         
        let nnerStatePlus : NSMutableDictionary = [:]
        nnerStatePlus[tensnBottomSpeech([409, 437, 443, 440, 425, 427])] = "4"
        nnerStatePlus[tensnBottomSpeech([429, 423, 435, 427, 421, 436, 423, 435, 427])] = "\(trncteOnceSublayer[tensnBottomSpeech([442, 431, 442, 434, 427])] ?? "")"
        nnerStatePlus[tensnBottomSpeech([429, 423, 435, 427, 421, 431, 426])] = "\(trncteOnceSublayer[tensnBottomSpeech([431, 426])] ?? "")"
        nnerStatePlus[tensnBottomSpeech([442, 447, 438, 427])] = (self.chrAmountCard.exmpleLongDamage == "1" ?  "4" : "1")
        nnerStatePlus[tensnBottomSpeech([442, 447, 438, 427, 376])] = lvlProduceRoot.default.selessLargeIncrement() ? "1" : "2"
        invntEquipmentCash().cmprseMouseWarn(dellcDivideMark: sscteWarnIndex("iptv_guide_sh"), prdceTrueView: nnerStatePlus as! [String : Any])
    }
    
    func nywhereComeEntry(dsmssSystemMoment:Bool){
        if (dsmssSystemMoment) {
            reserveCenterRegard.addObserver(self, forKeyPath: tensnBottomSpeech([441, 442, 423, 442, 443, 441]), options: .new, context: nil)
        } else if (reserveCenterRegard != nil) {
            reserveCenterRegard.removeObserver(self, forKeyPath: tensnBottomSpeech([441, 442, 423, 442, 443, 441]), context: nil)
        }
    }
    
    override func observeValue(forKeyPath keyPath: String?, of object: Any?, change: [NSKeyValueChangeKey : Any]?, context: UnsafeMutableRawPointer?) {
        if keyPath == tensnBottomSpeech([441, 442, 423, 442, 443, 441]), let welcmeDrumAware = object as? AVPlayerItem {
            if (welcmeDrumAware.status == .readyToPlay) {
                pltfrmLittleRight.stopAnimating()
                verwrteFlushDecoder.pause()
                thcknessCancelFont = true
                if (nmersRemainWeek) {
                    pctreButtonOmit.isHidden = true
                    verwrteFlushDecoder.play()
                    frscreCellsAround()
                }
            } else if (welcmeDrumAware.status == .failed) {
                pltfrmLittleRight.stopAnimating()
                verwrteFlushDecoder.pause()
                thcknessCancelFont = false
            }
        }
    }
    
    func nywhereModelCash(verwrteCareMacro:drivWord, sterskGigaExist:NSIndexPath?){
        self.chrAmountCard = verwrteCareMacro
        self.sterskGigaExist = sterskGigaExist
        tensnDictSwap()
    }
    
    public func mgnetcDeleteStep(){
        nmersRemainWeek = true
        self.isHidden = false
        rchveHoldSpace = TimeInterval(NSDate().timeIntervalSince1970)
        if (self.chrAmountCard.dsmssShieldTrue == "1") {
            envrnSecondReduce()
        }
        
         
        let trncteOnceSublayer:[String:Any] = self.chrAmountCard.pperLowestTerm[(self.sterskGigaExist == nil ? 0 : self.sterskGigaExist.row)]
        let nnerStatePlus : NSMutableDictionary = [:]
        nnerStatePlus[tensnBottomSpeech([409, 437, 443, 440, 425, 427])] = "2"
        nnerStatePlus[tensnBottomSpeech([429, 423, 435, 427, 421, 436, 423, 435, 427])] = "\(trncteOnceSublayer[tensnBottomSpeech([442, 431, 442, 434, 427])] ?? "")"
        nnerStatePlus[tensnBottomSpeech([429, 423, 435, 427, 421, 431, 426])] = "\(trncteOnceSublayer[tensnBottomSpeech([431, 426])] ?? "")"
        nnerStatePlus[tensnBottomSpeech([442, 447, 438, 427])] = (self.chrAmountCard.exmpleLongDamage == "1" ?  "4" : "1")
        nnerStatePlus[tensnBottomSpeech([442, 447, 438, 427, 376])] = lvlProduceRoot.default.selessLargeIncrement() ? "1" : "2"
        invntEquipmentCash().cmprseMouseWarn(dellcDivideMark: sscteWarnIndex("iptv_guide_sh"), prdceTrueView: nnerStatePlus as! [String : Any])
    }
    
    @objc public func ttlKnowNegate(){
        nmersRemainWeek = false
        self.isHidden = true
        if (self.chrAmountCard.dsmssShieldTrue == "1") {
            if (chnnelsHyphenLike != nil) {
                chnnelsHyphenLike.invalidate()
                chnnelsHyphenLike = nil
            }
        }
        let trncteOnceSublayer:[String:Any] = self.chrAmountCard.pperLowestTerm[(self.sterskGigaExist == nil ? 0 : self.sterskGigaExist.row)]
        let pnnCenterWatch = trncteOnceSublayer[tensnBottomSpeech([434, 431, 436, 433])]
        if (self.chrAmountCard.exmpleLongDamage == "1" && pnnCenterWatch != nil && !(pnnCenterWatch is NSNull) && pnnCenterWatch as! String != "") {
            tndexDistinctPurge()
        }
        self.removeFromSuperview()
    }
}
