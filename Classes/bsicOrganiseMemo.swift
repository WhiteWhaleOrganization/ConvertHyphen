 
 
 
 
 
 

import UIKit

public class bsicOrganiseMemo: rstRedefineYear {
    public var chrAmountCard:grnContentWait?
    public var nmersCharmCard:Int?
    
    public override func mgnetcDeleteStep() {
        guard let chrAmountCard = chrAmountCard else {return}
        var replceLogicWait = "";
        var temFooterLoss = "";
        var reqreReducePunch = "";
        var resltSharedClick = "";
        var pgrdeMatrixPrepend = "";
        var enclseBellRemove = "";
        if (nmersCharmCard == 1) {
            replceLogicWait = chrAmountCard.replceLogicWait;
            reqreReducePunch = chrAmountCard.rbySecondStep;
            resltSharedClick = chrAmountCard.descrbePlainObserve;
            pgrdeMatrixPrepend = chrAmountCard.chnnelCallMotif;
            enclseBellRemove = chrAmountCard.retreveDeleteUnpack;
        } else if (nmersCharmCard == 2) {
            temFooterLoss = chrAmountCard.drwerSafeLight;
            reqreReducePunch = chrAmountCard.descendSoftTarget;
            resltSharedClick = chrAmountCard.descrbePlainObserve;
            enclseBellRemove = chrAmountCard.fctryFlushPlay;
            if (chrAmountCard.bserveEntriesTeach.count > 0 && UIApplication.shared.canOpenURL(URL(string: chrAmountCard.bserveEntriesTeach)!)) {
                enclseBellRemove = chrAmountCard.regexOctetHighlight;
            }
        }
        
        self.backgroundColor = .lbrryRemainPath(trnsfrmUnitUser: 0x000000)
        let pperPeriodBoot = UIView.init()
        pperPeriodBoot.layer.backgroundColor = UIColor.lbrryRemainPath(trnsfrmUnitUser: 0x2C2B31).cgColor
        pperPeriodBoot.layer.cornerRadius = 12.auto()
        self.addSubview(pperPeriodBoot)
        pperPeriodBoot.snp.makeConstraints { make in
            make.center.equalToSuperview()
            make.width.equalTo(320.auto())
        }
        
        let cmpseWiseBaseline = UIImageView.init()
        cmpseWiseBaseline.kf.setImage(with:certnAgainProxy("247"))
        pperPeriodBoot.addSubview(cmpseWiseBaseline)
        cmpseWiseBaseline.snp.makeConstraints { make in
            make.top.equalTo(10)
            make.right.equalTo(-15)
            make.size.equalTo(100)
        }
        
        let keywrdTrueBegin = UILabel()
            .predctCrossKeep(0)
            .pertnCardMonth(replceLogicWait)
            .referenceCellPunch(.white)
            .ccrcyOnceInvent(.systemFont(ofSize: 22, weight: .bold))
            .ccdentPairScreen(replceLogicWait.count == 0)
        pperPeriodBoot.addSubview(keywrdTrueBegin)
        keywrdTrueBegin.snp.makeConstraints { make in
            make.top.equalTo(10)
            make.left.equalTo(15)
            make.right.equalTo(cmpseWiseBaseline.snp.left).offset(-10)
            if (temFooterLoss.count == 0) {
                make.height.equalTo(100)
            } else {
                make.height.equalTo(50)
            }
        }
        
        let bttnSpellMemo = UILabel()
            .predctCrossKeep(0)
            .pertnCardMonth(temFooterLoss)
            .referenceCellPunch(.lbrryRemainPath(trnsfrmUnitUser: 0x888888))
            .ccrcyOnceInvent(.systemFont(ofSize: 14, weight: .medium))
            .ccdentPairScreen(temFooterLoss.count == 0)
        pperPeriodBoot.addSubview(bttnSpellMemo)
        bttnSpellMemo.snp.makeConstraints { make in
            if (replceLogicWait.count == 0) {
                make.bottom.equalTo(cmpseWiseBaseline.snp.bottom).offset(0);
            } else {
                make.top.equalTo(keywrdTrueBegin.snp.bottom).offset(10);
            }
            make.left.equalTo(15);
            make.right.equalTo(cmpseWiseBaseline.snp.left).offset(-10);
            make.height.equalTo(40);
        }
        
        let mplementEffortInsert = UILabel()
            .predctCrossKeep(0)
            .pertnCardMonth(reqreReducePunch.count > 0 ? reqreReducePunch : (resltSharedClick.count > 0 ? resltSharedClick : (pgrdeMatrixPrepend.count > 0 ? pgrdeMatrixPrepend : "")))
            .referenceCellPunch(.white)
            .ccrcyOnceInvent(.systemFont(ofSize: 16, weight: .bold))
        pperPeriodBoot.addSubview(mplementEffortInsert)
        mplementEffortInsert.snp.makeConstraints { make in
            make.left.equalTo(15)
            make.right.equalTo(-15)
            if (bttnSpellMemo.isHidden) {
                make.top.equalTo(keywrdTrueBegin.snp.bottom).offset(10)
            } else {
                make.top.equalTo(bttnSpellMemo.snp.bottom).offset(10)
            }
        }
        
        let restConvertHyphen = UILabel()
            .predctCrossKeep(0)
            .pertnCardMonth(resltSharedClick)
            .referenceCellPunch(UIColor.lbrryRemainPath(trnsfrmUnitUser: 0x888888))
            .ccrcyOnceInvent(.systemFont(ofSize: 14, weight: .medium))
            .ccdentPairScreen((nmersCharmCard == 2 && chrAmountCard.cnsstExistsPerform == 1) ? (reqreReducePunch.count > 0 ? true : false) : (reqreReducePunch.count == 0 ? true : false))
        pperPeriodBoot.addSubview(restConvertHyphen)
        restConvertHyphen.snp.makeConstraints { make in
            make.left.equalTo(15)
            make.right.equalTo(-15)
            make.top.equalTo(mplementEffortInsert.snp.bottom).offset(20)
        }
        
        let sbscrptDetectWill = tchConfusePlus(type: .custom)
            .therefreComeAngle(enclseBellRemove)
            .rerrngeStatusTask(.systemFont(ofSize: 16, weight: .heavy))
            .ccdentAssignDisk(.black)
            .pltfrmBranchZero(certnAgainProxy("264"))
            .szeDictionaryRefresh(self, action: #selector(eventSmoothSequence))
        pperPeriodBoot.addSubview(sbscrptDetectWill)
        sbscrptDetectWill.snp.makeConstraints { make in
            if (restConvertHyphen.isHidden == true) {
                make.top.equalTo(mplementEffortInsert.snp.bottom).offset(20);
            } else {
                make.top.equalTo(restConvertHyphen.snp.bottom).offset(20);
            }
            make.left.equalTo(15);
            make.right.equalTo(-15);
            make.height.equalTo(50);
            make.bottom.equalTo(-20);
        }
        
        if chrAmountCard.cnsstExistsPerform == 2 {
            let pertnReachReplay = tchConfusePlus(type: .custom)
                .emlteEncodeRotate(UIEdgeInsets(top: 5, left: 5, bottom: 5, right: 5))
                .szeDictionaryRefresh(self, action: #selector(vergeTeamRename))
                .dscssPriceDeselected(certnAgainProxy("263"))
            self.addSubview(pertnReachReplay)
            pertnReachReplay.snp.makeConstraints { make in
                make.top.equalTo(pperPeriodBoot.snp.bottom).offset(30)
                make.size.equalTo(50)
                make.centerX.equalToSuperview()
            }
        }
        
         
        let nnerStatePlus : NSMutableDictionary = [:]
        nnerStatePlus[tensnBottomSpeech([442, 447, 438, 427])] = chrAmountCard.cnsstExistsPerform
        nnerStatePlus[tensnBottomSpeech([429, 421, 436, 423, 435, 427])] = chrAmountCard.explctGainStrong
        nnerStatePlus[tensnBottomSpeech([441, 437, 443, 440, 425, 427])] = "1"
        nnerStatePlus[tensnBottomSpeech([444, 431, 438, 421, 436, 423, 435, 427])] = lvlProduceRoot.default.selectnRentMagic()[tensnBottomSpeech([438, 375])]
        nnerStatePlus[tensnBottomSpeech([444, 431, 438, 421, 442, 447, 438, 427])] = lvlProduceRoot.default.selectnRentMagic()[tensnBottomSpeech([438, 376])]
        invntEquipmentCash().cmprseMouseWarn(dellcDivideMark: "225", prdceTrueView: nnerStatePlus as! [String : Any])
    }    
}
