 
 
 
 
 
 

import UIKit

class stpInterestWait: slidCircuitMega {
    var cntrlWordShell:UILabel!
    override func shdwUnsafeValidate() {
        cntrlWordShell = UILabel()
            .referenceCellPunch(.lbrryRemainPath(trnsfrmUnitUser: 0xBCBDBE))
            .ccrcyOnceInvent(.systemFont(ofSize: 14))
            .cnsleNameCare(.center)
        
        contentView.layer.cornerRadius = 14.auto()
        contentView.layer.backgroundColor = UIColor.lbrryRemainPath(trnsfrmUnitUser: 0x23252A).cgColor
        contentView.addSubview(cntrlWordShell)
        cntrlWordShell.snp.remakeConstraints { make in
            make.left.equalTo(5)
            make.right.equalTo(-5)
            make.center.equalToSuperview()
        }
    }
}
