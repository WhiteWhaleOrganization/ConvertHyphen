 
 
 
 
 
 

import UIKit

class dvicSeparateHeap: slidCircuitMega {
    var cntnFlagInput:UIImageView!
    override func shdwUnsafeValidate() {
        
        cntnFlagInput = UIImageView()
        
        contentView.layer.cornerRadius = 14.auto()
        contentView.layer.backgroundColor = UIColor.lbrryRemainPath(trnsfrmUnitUser: 0x23252A).cgColor
        contentView.addSubview(cntnFlagInput)
        cntnFlagInput.snp.remakeConstraints { make in
            make.center.equalToSuperview()
            make.size.equalTo(CGSizeMake(16, 16))
        }
    }
}
