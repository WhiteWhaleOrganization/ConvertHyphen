 
 
 
 
 
 

import UIKit

class dthSqueezeCome: rstRedefineYear {
    
    static var nmersRemainWeek:Bool = false
    var pperPeriodBoot:UIView!
    var chrAmountCard:btTraverseKind?
    var lternteSevenTrim:srchControlThen!
    func nshftSearchTape(chrAmountCard:btTraverseKind?) {
        self.chrAmountCard = chrAmountCard
        keywrdNameSince()
    }
    override func keywrdNameSince() {
        if let chrAmountCard = chrAmountCard{
            self.backgroundColor = .lbrryRemainPath(trnsfrmUnitUser: 0x000000, ntteBelowNest: 0.5)
            let crrptFancyBlue = UIBlurEffect(style: .dark)
            let shrtctYellowMach = UIVisualEffectView(effect: crrptFancyBlue)
            shrtctYellowMach.alpha = 0.5
            self.addSubview(shrtctYellowMach)
            shrtctYellowMach.snp.remakeConstraints { make in
                make.edges.equalToSuperview()
            }
            
            pperPeriodBoot = UIView()
            self.addSubview(pperPeriodBoot)
            pperPeriodBoot.snp.remakeConstraints { make in
                make.center.equalToSuperview()
                make.left.equalTo(20.auto())
                make.right.equalTo(-20.auto())
            }
            
            let cntnFlagInput = UIImageView()
                .pnnTracePath(true)
                .estercIncrementCaret(.scaleAspectFill)
                .descrbeDetailObserve(true)
                .frscrePeriodZero(8)
                .rtneDivideCreate(UIColor.white.cgColor)
                .versnBoardFree(0.5)
                .dscssPriceDeselected(URL(string: chrAmountCard.nknwnKiloPseudo))
            pperPeriodBoot.addSubview(cntnFlagInput)
            cntnFlagInput.snp.remakeConstraints { make in
                make.top.left.right.equalTo(0)
                make.height.equalTo(479.auto())
            }
            cntnFlagInput.layoutIfNeeded()
            let fcsDecoderBound = CAGradientLayer()
            fcsDecoderBound.colors = [
              UIColor(red: 0, green: 0, blue: 0, alpha: 0).cgColor,
              UIColor(red: 0, green: 0, blue: 0, alpha: 0.6).cgColor
            ]
            fcsDecoderBound.locations = [0, 1]
            fcsDecoderBound.startPoint = CGPoint(x:0.5, y: 0)
            fcsDecoderBound.endPoint = CGPoint(x: 0.5, y: 1)
            fcsDecoderBound.bounds = cntnFlagInput.bounds
            fcsDecoderBound.position = cntnFlagInput.center
            cntnFlagInput.layer.addSublayer(fcsDecoderBound)

            
            let nderlneSizeBlacklist = UIImageView().dscssPriceDeselected(certnAgainProxy("353"))
            pperPeriodBoot.addSubview(nderlneSizeBlacklist)
            nderlneSizeBlacklist.snp.remakeConstraints { make in
                make.top.equalTo(cntnFlagInput.snp.bottom).offset(30)
                make.size.equalTo(CGSizeMake(40, 40))
                make.bottom.equalToSuperview()
                make.centerX.equalToSuperview()
            }
            nderlneSizeBlacklist.isUserInteractionEnabled = true
            nderlneSizeBlacklist.addGestureRecognizer(UITapGestureRecognizer(target: self, action: #selector(ttlKnowNegate)))
            
            
            let crctFreeBlack = srchControlThen()
            crctFreeBlack.refreshngDeskPseudo = true
            crctFreeBlack.cnImplementUsage.kf.setImage(with:  certnAgainProxy("355"))
            crctFreeBlack.keywrdTrueBegin.text = selectDeflateKind(232)
            crctFreeBlack.verllWrongDrag = CGSizeMake(24, 24)
            crctFreeBlack.preserveInvokeRegard = .init(top: 0, left: 36.auto(), bottom: 0, right: 0)
            crctFreeBlack.smmryShortSkip = .init(top: 10.auto(), left: 10.auto(), bottom: 10.auto(), right: 0.auto())
            crctFreeBlack.keywrdTrueBegin.textColor = .lbrryRemainPath(trnsfrmUnitUser: 0x111218)
            crctFreeBlack.keywrdTrueBegin.font = .systemFont(ofSize: 18,weight: .semibold)
            crctFreeBlack.layer.backgroundColor = UIColor.lbrryRemainPath(trnsfrmUnitUser: 0xFFFFFF).cgColor
            crctFreeBlack.layer.cornerRadius = 5
            crctFreeBlack.bndryMuchDesign = 0
            crctFreeBlack.setNeedsUpdateConstraints()
            crctFreeBlack.isUserInteractionEnabled = true
            crctFreeBlack.addGestureRecognizer(UITapGestureRecognizer(target: self, action: #selector(verflwBothClient)))
            cntnFlagInput.addSubview(crctFreeBlack)
            crctFreeBlack.snp.remakeConstraints { make in
                make.left.equalTo(20.auto())
                make.bottom.equalTo(-20.auto())
            }
            
            let lternteSevenTrim = srchControlThen()
            lternteSevenTrim.refreshngDeskPseudo = true
            lternteSevenTrim.cnImplementUsage.kf.setImage(with: (lvlProduceRoot.default.sbscrptSheetWare(nstntEveryShared: chrAmountCard.nstntEveryShared) == nil) ? certnAgainProxy("356") : certnAgainProxy("354"))
            lternteSevenTrim.keywrdTrueBegin.text = tensnBottomSpeech([403, 447, 358, 402, 431, 441, 442])
            lternteSevenTrim.verllWrongDrag = CGSizeMake(24, 24)
            lternteSevenTrim.preserveInvokeRegard = .init(top: 0, left: 24.auto(), bottom: 0, right: 0)
            lternteSevenTrim.smmryShortSkip = .init(top: 10.auto(), left: 10.auto(), bottom: 10.auto(), right: 0.auto())
            lternteSevenTrim.keywrdTrueBegin.textColor = .lbrryRemainPath(trnsfrmUnitUser: 0xFFFFFF)
            lternteSevenTrim.keywrdTrueBegin.font = .systemFont(ofSize: 18,weight: .semibold)
            lternteSevenTrim.layer.backgroundColor = UIColor.lbrryRemainPath(trnsfrmUnitUser: 0x272727,ntteBelowNest: 0.35).cgColor
            lternteSevenTrim.layer.cornerRadius = 5
            lternteSevenTrim.layer.borderColor = UIColor.lbrryRemainPath(trnsfrmUnitUser: 0xECECEC).cgColor
            lternteSevenTrim.layer.borderWidth = 1
            lternteSevenTrim.bndryMuchDesign = 0
            lternteSevenTrim.setNeedsUpdateConstraints()
            lternteSevenTrim.addGestureRecognizer(UITapGestureRecognizer(target: self, action: #selector(lredyTreeThink)))
            cntnFlagInput.addSubview(lternteSevenTrim)
            lternteSevenTrim.snp.remakeConstraints { make in
                make.right.equalTo(-20.auto())
                make.bottom.equalTo(-20.auto())
                make.left.equalTo(crctFreeBlack.snp.right).offset(10.auto())
                make.width.equalTo(crctFreeBlack.snp.width)
            }
            self.lternteSevenTrim = lternteSevenTrim
        }
    }    
}
